class Sum(object):

    def __init__(self, left, left_done, right, right_done):
        self.left = left
        self.left_done = left_done
        self.right = right
        self.right_done = right_done
